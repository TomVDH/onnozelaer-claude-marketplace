@AGENTS.md

# Claude-specific overrides

Repository conventions, plugin inventory, build/validate, and commit style all live in `AGENTS.md` (imported above). Any agent — Claude, Gemini, Codex, Cursor — reads from there.

This file is for **Claude Code-specific overrides only**:
- Slash-command behavior hints
- Plugin/skill invocation preferences
- Claude-only tool guidance

**If you're about to add generic repo context here, move it to `AGENTS.md` instead.**
